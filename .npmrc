# NPM Configuration for Web-only React project
# Force legacy peer dependency resolution
legacy-peer-deps=true

# Disable React Native package installation
save-exact=false

# Use npm's legacy peer dependency algorithm
auto-install-peers=false

# Ignore peer dependency warnings
fund=false
audit=false
